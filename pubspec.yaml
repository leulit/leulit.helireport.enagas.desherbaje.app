name: helireport_desherbaje
description: App móvil para operadores de campo - Módulo Desherbaje Enagas
publish_to: 'none'
version: 1.2.0+120

environment:
  sdk: '^3.12.1'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  camera: ^0.12.0+1
  chat_bubbles: ^1.10.1  
  connectivity_plus: ^7.3.0
  crypto: ^3.0.7
  dio: ^5.11.0
  flutter_map: ^8.3.1
  flutter_map_compass: ^1.1.1
  flutter_map_location_marker: ^10.3.0
  flutter_secure_storage: ^11.0.0  
  get: ^4.7.3
  image_picker: ^1.2.3
  # Necesarios para activar el Photo Picker de Android (ver main.dart).
  # Cotas solidarias con image_picker: subir junto a él.
  image_picker_android: ^0.8.13
  image_picker_platform_interface: ^2.11.0
  video_player: ^2.13.0
  gal: ^2.3.3
  latlong2: ^0.9.1 # pinned: flutter_map_location_marker 10.3.0 requires ^0.9.1

  leulit_flutter_actionmanager: ^5.9.0
  leulit_flutter_dependency_injection: ^0.1.1
  leulit_flutter_fullresponsive: ^3.0.0
  leulit_pipeline_pattern:
    path: /Users/emiloalvarez/Work/PROYECTOS/CUSTOM-LIBRARIES-FLUTTER/WORKFLOW-MANAGER/leulit_pipeline_pattern  

  logger: ^2.7.0
  path: ^1.9.1
  permission_handler: ^13.0.1
  shared_preferences: ^2.5.5
  sqflite: ^2.4.3
  intl: ^0.20.3
  uuid: ^4.6.0
  url_launcher: ^6.3.2
  flutter_foreground_task: ^10.0.0
  geolocator: ^14.0.3
  flutter_rotation_sensor: ^0.3.0
  device_info_plus: ^13.2.0
  package_info_plus: ^10.2.1
  supercluster: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
  sqflite_common_ffi: ^2.4.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
